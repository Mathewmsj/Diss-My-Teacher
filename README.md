# Rate My Teacher

一个老师评分系统，学生可以为老师进行Tier等级评分（T1、T2、T3）。

## 功能特性

- 🎯 老师评分：学生可以选择老师并给予T1、T2、T3等级评分
- 📝 评分理由：每个评分需要10-50字的理由说明
- ⏰ 每日限制：每个学生每天最多为2位老师评分
- 📊 排行榜：展示老师排名，支持当天、当月、当学期、当学年和历史总排名
- 🔑 关键词提取：自动提取评论中的关键词
- 👍 互动功能：支持对评分进行点赞和点踩
- 🎨 精美UI：类似虎扑风格的界面设计，六边形徽章展示
- 🔐 Mock 登录 / 注册：基于本地 mock JSON 数据的登录注册
- 👤 个人中心：查看当前账号、投票次数、我的评分

## 技术栈

- Vue 3
- Vue Router
- Vite
- LocalStorage (数据存储)
- Mock JSON 数据（`src/mock/*.json`）

## 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

应用将在 http://localhost:3000 打开

### 构建生产版本

```bash
npm run build
```

## 评分规则

- **T1**: 5分
- **T2**: 10分
- **T3**: 15分

分数越高，排名越靠前。

## 使用说明

1. 在首页选择要评分的老师
2. 选择Tier等级（T1/T2/T3）
3. 填写10-50字的评分理由
4. 提交评分
5. 在排行榜页面查看老师排名
6. 点击老师查看详细评分和互动
7. 登录 / 注册（使用 mock 数据，示例：`alice@example.com / password123`）
8. 个人中心查看我的评分、今日投票次数

## Mock 数据与页面

- 数据文件：`src/mock/teachers.json` / `ratings.json` / `users.json`
- 初始化：首次加载会把 mock 数据写入 localStorage
- 页面：
  - `/login` 登录
  - `/signup` 注册
  - `/profile` 个人中心
  - `/` 首页评分
  - `/ranking` 排行榜
  - `/teacher/:id` 评分详情

## 简易 ER / Schema（与 mock 数据一致）

- User(id, name, email, password)
- Teacher(id, name, department)
- Rating(id, teacherId, userId, tier, reason, createdAt, likes, dislikes)
- UserVote(date -> [teacherId])（本地限流用）
- UserInteraction(ratingId -> like/dislike)
